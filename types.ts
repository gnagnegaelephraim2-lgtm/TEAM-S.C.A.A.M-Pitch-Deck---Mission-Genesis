
export interface TeamMember {
  name: string;
  role: string;
  country: string;
  image: string;
}

export interface MarketData {
  name: string;
  value: number;
  label: string;
}

export interface BusinessTier {
  name: string;
  price: string;
  features: string[];
}
